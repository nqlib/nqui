/**
 * Library package — no Vite showcase app.
 * Visual catalog: ../nqui-showcase (`pnpm nqui:local` then `pnpm dev`).
 */
console.info(
  "[nqui] Component catalog lives in nqui-showcase. Run: cd ../nqui-showcase && pnpm nqui:local && pnpm dev → /catalog",
)
